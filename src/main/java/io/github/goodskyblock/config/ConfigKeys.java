package io.github.goodskyblock.config;

public class ConfigKeys {

    protected static ConfigKey<Boolean> newBooleanKey(String section, Boolean defaultValue) {
        return new ConfigKey<>();
    }
}
